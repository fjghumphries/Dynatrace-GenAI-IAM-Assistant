================================================================================
DYNATRACE IAM POLICIES REFERENCE
================================================================================
Generated: 2026-04-30
Configuration: 3 BUs, 3 Applications, 2 Stages

================================================================================
DEFAULT POLICIES (Data Sources — 8)
================================================================================

  1. Standard User
     - Base feature access for all groups
     - Includes: documents CRUD, automation read + SIMPLE write, Davis AI,
       SLO read, settings read (UNCONDITIONAL), notifications, extensions read

  2. Read Logs          — Grail bucket access for dt.logs table
  3. Read Metrics       — Grail bucket access for dt.metrics table
  4. Read Spans         — Grail bucket access for dt.spans table
  5. Read Events        — Grail bucket access for dt.events table
  6. Read BizEvents     — Grail bucket access for dt.bizevents table
  7. Read Entities      — Grail bucket access for dt.entities table
  8. Read System Events — Audit and system event access (no scoping)

  NOTE: Admin User is intentionally NOT used. It grants unconditional
  settings:objects:write that cannot be scoped. See gotcha #16.

================================================================================
TEMPLATED POLICIES (Parameterised Resources — 2)
================================================================================

  1. Scoped Grail Data Read
     - WHERE storage:dt.security_context startsWith bindParam
     - Tables: logs, metrics, spans, events, bizevents, entities
     - Provides record-level filtering (defense-in-depth)
     - Used only at BU level (app level uses boundaries on default policies)

  2. Scoped Settings Write
     - WHERE settings:dt.security_context startsWith bindParam
     - Grants: settings:objects:read, settings:objects:write, settings:schemas:read
     - ONLY source of settings:objects:write in this config
     - Used at both BU and Application admin level

================================================================================
CUSTOM POLICIES (Resources — 4)
================================================================================

  1. Admin Features (No Settings Write)
     - For BU Admins
     - Grants: automation admin, SLO write, extensions write, App Engine management
     - Does NOT include settings:objects:write or openpipeline:* (old API removed)
     - These permissions are tenant-wide (gotcha #20)

  2. OpenPipeline Management
     - For BU Admins only
     - Grants: settings:objects:write WHERE settings:schemaId = "builtin:openpipeline.<signal>.pipelines"
     - Covers all signal types: bizevents, davis.events, davis.problems, events,
       events.sdlc, events.security, logs, metrics, security.events, spans,
       system.events, user.events, usersessions (13 statements)
     - Intentionally excludes .routing and .pipeline-groups schemas (gotcha #22)
     - Uses Settings 2.0 API (replaces old openpipeline:configurations:* permissions)

  3. Anomaly Detection Write
     - For ALL groups (BU Admins, BU Users, App Admins, App Users)
     - Grants: settings:objects:write WHERE settings:schemaGroup = "group:anomaly-detection"
     - Unbounded — schemaGroup condition limits scope to anomaly detection schemas
     - Allows all users to create/modify anomaly detectors

  4. SLO Manager
     - For Application Admins
     - Grants: slo:slos:read/write, slo:objective-templates:read
     - Lighter than Admin Features — only SLO management

================================================================================
TOTALS
================================================================================
  Default policies referenced:  8
  Templated policies created:   2
  Custom policies created:      4
  ---
  Total policy references:     14

  At scale (10 BUs, 2000 apps): Same 14 policies — templates reuse via parameters
